{
  "name": "roomie-interior-coordinator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "사진의 빛과 색을 브라우저에서 분석하는 인테리어 콘셉트 코디네이터",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.js",
    "prestart": "npm run build",
    "start": "node scripts/serve.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
